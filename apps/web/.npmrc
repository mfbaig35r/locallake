# pnpm 11+ defaults `minimum-release-age` to ~24h, which blocks installs of
# freshly published transitive deps (e.g. point releases of @tanstack/react-query
# that land between when we lock the file and when CI / Docker builds run).
# We commit the lockfile and trust what's in it, so disable the policy.
minimum-release-age=0
